import React from 'react';

import { UploadTxnModal } from '../../features/CategorizeTransactions';

describe('Upload Transaction Modal', () => {
  it.todo('Renders without error');

  describe('Setting up transaction data', () => {
    it.todo('Dispatches the correct uploadable transactions on submit');

    it.todo('Changes how it processes data if onColumnAmounts is checked');
  });
});
